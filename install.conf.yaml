- clean: ['~']

- link:
    ~/.gitconfig: .gitconfig
    ~/.gitignore: .gitignore
    ~/.zshrc: .zshrc
    ~/.oh-my-zsh/cutom/themes:
      create: true
      path: ohmyzsh/themes/
    ~/.atom/keymap.cson:
      create: true
      path: atom/keymap.cson

- shell:
  - [git submodule update --init --recursive, Installing submodules]
